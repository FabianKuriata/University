#pragma once
#include <iostream>
#include <string>

using namespace std;

static bool isOperator2arg(string val);
static bool isInt(string val);
static bool isDouble(string val);
static bool isOperator3arg(string val);
static bool isWord(string val);

